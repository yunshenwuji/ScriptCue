services:
  scriptcue-server:
    build: .
    image: scriptcue-server
    ports:
      - "8000:8000"
    volumes:
      - scriptcue-data:/app/data
    restart: unless-stopped

volumes:
  scriptcue-data:
