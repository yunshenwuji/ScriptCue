开发启动：`cd server && python -m uvicorn server.main:app --host 0.0.0.0 --port 8000`；Docker 构建需在仓库根目录执行 `docker build -f server/Dockerfile -t scriptcue-server .`；运行时通过环境变量 `SC_DATA_DIR`（审计日志目录）、`SC_CONTROLLER_DIR`（主控前端目录）、`SC_DEFAULT_LEAD_MS`（默认指令提前量）、`SC_LOG_LEVEL` 配置行为。