GUI 版可通过 `python -m agent` 或 `python scriptcue_agent.py` 启动；CLI 调试版使用 `python -m agent.cli --server ws://... --room AB12CD --nickname 昵称 [--dry-run]`；macOS 首次运行需授予「辅助功能」权限，否则 `KeySender.check()` 会失败并弹出系统设置引导；打包命令见 `build_windows.ps1` / `build_macos.sh`。