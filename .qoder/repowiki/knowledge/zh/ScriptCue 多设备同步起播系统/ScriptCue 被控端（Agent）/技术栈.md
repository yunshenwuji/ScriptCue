Python 3 + websockets 异步 WebSocket 客户端；tkinter 作为 GUI 框架；Windows 下通过 ctypes 调用 Win32 SendInput 注入空格键，macOS 下通过 pynput 调用 CoreGraphics CGEventPost；PyInstaller 打包脚本位于 `build_windows.ps1` 与 `build_macos.sh`。