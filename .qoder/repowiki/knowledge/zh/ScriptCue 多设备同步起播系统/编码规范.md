- 跨模块消息结构以 docs/protocol.md 为准，server 与 agent 各自维护一份等价的 protocol.py 常量定义以保持类型一致。
- 各子模块通过独立的 requirements.txt 声明依赖，可单独 pip install 运行，不共享 Python 包。
- WebSocket 交互围绕「房间」概念组织：主控端与被控端均需指定 room 码加入同一会话后再收发指令。